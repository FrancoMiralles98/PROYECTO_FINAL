

const socket = io()

/* document.addEventListener('click', (e)=>{
    
    
    if(e.target.innerText == 'Agregar'){
        socket.emit('agregar',{
            CarritoId: e.target.value,
            codigo: e.target.id,
            cantidad: 1
        })
    }
}) */